import {StyleSheet} from 'react-native';
import {GREY_LIGHT} from '../../vars';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: GREY_LIGHT,
  },
  layout: {
    height: '100%',
    width: '100%',
    backgroundColor: GREY_LIGHT,
    padding: 12,
  },
});
